import {
  formateTime,
  formateDate,
  calcTimes,
  calcSeconds
} from './date/index'

import other from './other/index'
const to = other.to
const omit = other.omit

export {
  formateTime,
  formateDate,
  calcTimes,
  calcSeconds,
  to,
  omit
}